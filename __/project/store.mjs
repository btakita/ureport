import {_mixin__store, compute} from 'ctx-core/store/lib.mjs'
import {mixin} from '__/object/lib.mjs'
export const __store__project = _mixin__store('__store__project', store => {
	mixin(store, {
		reset__project() {

		}
	})
})
